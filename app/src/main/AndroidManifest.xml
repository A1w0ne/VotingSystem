<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VotingSystem"
        tools:targetApi="31">

        <!-- 1) Launch / Login -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2) Dashboard (if you have it) -->
        <activity android:name=".DashboardActivity" />

        <!-- 3) Profile -->
        <activity android:name=".ProfileActivity" />

        <!-- 4) Election List -->
        <activity android:name=".ElectionActivity" />

        <!-- 5) President Voting -->
        <activity android:name=".PresidentActivity" />

        <!-- 6) Vice-President Voting -->
        <activity android:name=".VicepresidentActivity" />

        <!-- 7) Secretary Voting -->
        <activity android:name=".SecretaryActivity" />

        <!-- 8) Preview Your Choices -->
        <activity android:name=".PreviewActivity" />

        <!-- 9) Vote Confirmation -->
        <activity android:name=".VoteconfirmationActivity" />

    </application>
</manifest>
